using UnityEngine;
using System.Collections;

public class Envi_Particles_Death : MonoBehaviour {

	// Use this for initialization
	void Start () {
		Destroy (this.gameObject, 1f);
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
